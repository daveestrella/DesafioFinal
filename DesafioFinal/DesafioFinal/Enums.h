#ifndef ENUMS_H
#define ENUMS_H

enum class TipoPrenda { Camisa = 1, Pantalon = 2 };

enum class TipoCuello { Comun = 1, Mao = 2 };

enum class TipoManga { Corta = 1, Larga = 2 };

enum class TipoPantalon { Comun = 1, Chupin = 2 };

enum class TipoCalidad { Standard = 1, Premium = 2 };

#endif
